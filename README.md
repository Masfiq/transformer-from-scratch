# transformer-from-scratch

Full Transformer encoder-decoder architecture implemented from scratch in PyTorch, including scaled dot-product attention, multi-head attention, positional embeddings, and position-wise feedforward layers. Trained on WikiText2 for masked language modeling and used to analyze how contextual word embeddings evolve across training epochs via cosine similarity.
